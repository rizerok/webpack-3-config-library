{
  "name": "webpack-3-config-library",
  "version": "0.0.1",
  "description": "native config with webpack 3",
  "main": "dist/app.js",
  "scripts": {
    "build:dev":"webpack --progress --env=dev --display-modules",
    "build:lib":"webpack --progress --env=lib --display-modules",
    "watch":"webpack --watch --progress --env=dev"
  },
  "author": "alex",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^3.4.1",
    "webpack-merge": "^4.1.0"
  }
}
